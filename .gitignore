# Project-local virtual environment and caches (never committed)
.venv/
.pip-cache/

# Local toolchain bootstrapped on this machine (not deliverables)
.git-portable/
.python313/
.PortableGit.7z.exe
.python_nuget.zip
.viewcheck/
Dataset/*.exe

# Ground-truth / large video sets (not distributed)
Dataset/true_label/

# Large media and generated artifacts (never committed).
# NOTE: ignore the *contents* of processed_videos with /* (not the dir itself),
# so tracked deliverables below can be re-included explicitly.
Dataset/processed_videos/*
Dataset/raw_videos/
Dataset/by_signer/
Dataset/models/

# Tracked deliverables under processed_videos/ (labels + metadata, no .mp4s)
!Dataset/processed_videos/labels_split.xlsx
!Dataset/processed_videos/metadata/
Dataset/processed_videos/metadata/embeddings.npz
Dataset/processed_videos/metadata/clip_store.json
Dataset/processed_videos/metadata/clip_store_embeddings.npz
Dataset/processed_videos/metadata/signer_registry.json

Dataset/labels/vsl400/*.zip

# Python bytecode
__pycache__/
*.pyc

# Editor / tooling workspace metadata (not part of the deliverable)
.kiro/
.vscode/
.idea/

# Local-only assistant / scratch helpers (never committed)
HUONG_DAN_CHI_TIET.md
plan.txt
codex-api-on.bat
codex-api-on.ps1
codex-api-off.bat
codex-api-off.ps1
start-codex.bat
start-codex.ps1
run_custom.py
run_*.py

# Large datasets / derived deliverable folders (not distributed via git)
Curation/
VSL Keypoint Dataset (QIPEDC-derived) - Public/

# Cloned external repo (keypoint extraction/preprocess) + local staging
external/

# Tooling caches (kept on D:, never committed)
.pytest_cache/
.hypothesis/

# Run logs, intermediate artifacts, and large reference docs
Dataset/logs/
Dataset/extracted_keypoints/
*.pdf
