model:
  arch: spoter
  pretrained: experiments/spoter_m4_cam1
  num_frames: 96
  hidden_dim: 108
inference:
  source: webcam
  output_dir: demo/spoter_m4_cam1_webcam
  use_onnx: False
  device: cpu
  show_skeleton: True
  visualize: True
  top_k: 3
